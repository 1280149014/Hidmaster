<?xml version="1.0" encoding="utf-8"?><!-- 粉色弹窗背景 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginHorizontal="@dimen/normal_margin4"
    android:background="@drawable/dialog_bg"
    android:orientation="horizontal"
    android:padding="16dp"
    android:visibility="gone"
    tools:visibility="visible">

    <!-- 左侧图标 -->
    <ImageView
        android:id="@+id/iv_warning2"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="#FFFFFF"
        android:clipToOutline="true"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:shape="oval"
        android:src="@drawable/ic_bluetooth_disabled"
        app:tint="#FF5252"
        tools:targetApi="31" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/normal_margin"
        android:orientation="vertical">

        <!-- 弹窗标题 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="@string/bluetooth_connection_failed"
            android:textColor="#B71C1C"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- 提示信息 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/normal_margin"
            android:text="@string/remote_device_state_notice"
            android:textColor="#D32F2F"
            android:textSize="16sp" />

        <!-- Connect按钮 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:background="@drawable/button_bg"
            android:paddingHorizontal="@dimen/normal_margin"
            android:paddingVertical="12dp"
            android:background="@drawable/button_bg"
            android:text="@string/connect"
            android:textColor="#B71C1C"
            android:textSize="16sp" />

    </LinearLayout>
</LinearLayout>
