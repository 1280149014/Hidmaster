<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- 主内容区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- res/layout/activity_main.xml -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:navigationIconTint="@color/black">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- 左侧：菜单按钮（汉堡图标） -->
                <ImageView
                    android:id="@+id/toolbar_menu"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:clickable="true"
                    android:focusable="true"
                    android:contentDescription="菜单"
                    android:paddingHorizontal="16dp"
                    android:visibility="gone"
                    android:scaleType="center"
                    android:src="@drawable/ic_menu"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <!-- 右侧：图标组（键盘 + 设置） -->
                <ImageView
                    android:id="@+id/toolbar_setting"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:clickable="true"
                    android:focusable="true"
                    android:contentDescription="设置"
                    android:paddingHorizontal="16dp"
                    android:scaleType="center"
                    android:src="@drawable/ic_settings"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <ImageView
                    android:id="@+id/toolbar_keyboard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:clickable="true"
                    android:focusable="true"
                    android:contentDescription="键盘"
                    android:paddingHorizontal="16dp"
                    android:scaleType="center"
                    android:src="@drawable/ic_keyboard"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toStartOf="@+id/toolbar_setting"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintMarginEnd="8dp" /> <!-- 增加右侧图标间距，避免重叠 -->

                <!-- 中间：标题 + 状态文本（垂直排列，去除冗余 LinearLayout） -->
                <TextView
                    android:id="@+id/toolbar_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="Mouse / Keyboard"
                    android:layout_marginTop="@dimen/normal_margin"
                    android:textColor="@android:color/black"
                    android:textSize="16sp"
                    app:layout_constraintEnd_toStartOf="@+id/toolbar_keyboard"
                    app:layout_constraintStart_toEndOf="@+id/toolbar_menu"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintHorizontal_center_horizontal="true" /> <!-- 兜底居中，视觉更美观 -->

                <TextView
                    android:id="@+id/select_device_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="TCL 55C78-0827 connecting"
                    android:textColor="@android:color/darker_gray"
                    android:textSize="12sp"
                    app:layout_constraintStart_toStartOf="@+id/toolbar_title"
                    app:layout_constraintTop_toBottomOf="@+id/toolbar_title"
                    app:layout_constraintMarginTop="2dp" />

                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="2dp"
                    android:scaleType="centerInside"
                    android:src="@drawable/array_down"
                    app:layout_constraintBottom_toBottomOf="@+id/select_device_name"
                    app:layout_constraintStart_toEndOf="@+id/select_device_name"
                    app:layout_constraintTop_toTopOf="@+id/select_device_name" />

                <!-- 加载条：修正约束，调整高度，优化缩放属性，默认隐藏 -->
                <View
                    android:id="@+id/loading_bar"
                    android:layout_width="match_parent"
                    android:layout_height="2dp"
                    android:background="@color/blue1"
                    android:pivotX="0dp"
                    android:scaleX="1"
                    android:visibility="visible"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"/>

            </androidx.constraintlayout.widget.ConstraintLayout>

        </com.google.android.material.appbar.MaterialToolbar>

        <!-- Fragment容器 -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <FrameLayout
                android:id="@+id/fragment_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <include
                android:id="@+id/bluetooth_enable_prompt"
                layout="@layout/dialog_bluetooth_prompt"/>

            <include
                android:id="@+id/bluetooth_Not_Connected_prompt"
                layout="@layout/dialog_bluetooth_not_connected_prompt"/>

            <include
                android:id="@+id/bluetooth_Connect_failed_prompt"
                layout="@layout/dialog_bluetooth_connect_failed"/>
        </RelativeLayout>

    </LinearLayout>

    <!-- 左侧导航菜单 -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/nav_menu" />

</androidx.drawerlayout.widget.DrawerLayout>